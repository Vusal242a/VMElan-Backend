@model Final.Models.Entity.Agents

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Ad1000/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row">        
        <h3>Are you sure you want to delete this Agent?</h3>
        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    <img src="@(string.IsNullOrWhiteSpace(Model.Image)?"":Href("~/Template/Media/"+Model.Image))" style="width:400px;height:300px" id="news-image" />
                </dt>

                <dt>
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Phone)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Phone)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Email)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Email)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete Agent" class="btn" style="margin:5px !important; padding:0 !important; background-color:#9a0007; color:white;" />
                </div>
                <a class="btn btn-primary" style="margin-top:5px;" href="@Url.Action("Index","Agents")"> Back to Agents</a>
            }
        </div>

    </div>
</div>

